# 001 - 基盤セットアップ

ルートレイアウト・グローバルCSS変数・フォント設定を行い、アプリ全体の土台を作る。

## 対象ファイル

- `app/layout.tsx`
- `app/globals.css`

---

## Todo

- [×] `app/layout.tsx` を作成
  - [×] Noto Sans JP / Bebas Neue を `next/font/google` で読み込む
  - [×] `<html lang="ja">` を設定
  - [×] フォントを CSS 変数として body に適用する
  - [×] `<head>` に必要なメタ情報（タイトル・description）を設定
- [×] `app/globals.css` を作成
  - [×] カラーパレットを CSS 変数として定義する
    - [×] `--bg` / `--surface` / `--surface2` / `--border`
    - [×] `--accent` / `--accent2`
    - [×] `--text` / `--text-muted` / `--danger`
    - [×] `--j1` / `--j2` / `--j3`（リーグバッジ色）
  - [×] ポジション配色（GK/DF/MF/FW）を変数またはユーティリティとして定義
  - [×] body・* のベーススタイル（box-sizing, margin, font-family など）を設定
  - [×] Tailwind は使用しない（CSSのみ）
