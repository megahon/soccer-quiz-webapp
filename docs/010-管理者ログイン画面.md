# 010 - 管理者ログイン画面

`/admin` にアクセスした未認証ユーザーに表示するログインフォームを実装する。

## 対象ファイル

- `components/AdminLogin.tsx`

---

## Todo

- [×] `components/AdminLogin.tsx` を作成（`"use client"`）
  - [×] パスワード入力フォームを実装
  - [×] 送信時に `POST /api/auth` へ `{ action: 'login', password }` を fetch
  - [×] 認証成功後にページをリロード（親コンポーネントが `AdminPanel` を表示するよう切り替わる）
  - [×] 認証失敗時はエラーメッセージを表示（Toast または inline）
  - [×] ローディング中はボタンを非活性にする
  - [×] デザインは `--surface` / `--accent` のカラー変数を使う
  - [×] パスワードフィールドは `type="password"`
