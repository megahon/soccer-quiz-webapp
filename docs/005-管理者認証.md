# 005 - 管理者認証

パスワードによる管理者認証を実装する。HTTP-only Cookie でセッションを管理し、24 時間有効。

## 対象ファイル

- `app/api/auth/route.ts`
- `lib/auth.ts`

---

## Todo

- [×] `lib/auth.ts` を作成
  - [×] `isAuthenticated(request)` — Cookie を読み取りセッションの有効性を検証する関数
  - [×] セッション Cookie 名・有効期限（24時間）を定数で定義
- [×] `app/api/auth/route.ts` を作成
  - [×] `POST` ハンドラを実装
    - [×] `action: 'login'` — `ADMIN_PASSWORD` と照合し、一致したら Cookie をセット（HTTP-only, Secure, SameSite=Strict）
    - [×] `action: 'logout'` — Cookie を削除してセッションを破棄
    - [×] パスワード不一致の場合は 401 を返す
  - [×] `ADMIN_PASSWORD` は `process.env.ADMIN_PASSWORD` から読む（環境変数未設定時はエラー）
  - [×] `cookies()` を `await` して使う（Next.js このバージョンの破壊的変更）
