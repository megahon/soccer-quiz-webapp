# 019 - トップページ

アプリのエントリーポイント。サーバーサイドでデータを取得し、クライアントコンポーネントの `QuizApp` に渡す。

## 対象ファイル

- `app/page.tsx`

---

## Todo

- [×] `app/page.tsx` を作成（Server Component）
  - [×] `GET /api/teams` と `GET /api/players` のデータをサーバーサイドで取得
    - [×] Supabase 未設定時は `defaultData` から読み込む
  - [×] 取得したデータを `<QuizApp teams={...} players={...} />` に props として渡す
  - [×] お知らせデータも取得して `QuizApp` または バナーコンポーネントに渡す
  - [×] `<QuizApp />` はクライアントコンポーネントなので `"use client"` 側で state 管理する
