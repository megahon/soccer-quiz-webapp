# 002 - 型定義と DB スキーマ

アプリ全体で使う TypeScript 型と Supabase テーブル定義を作成する。

## 対象ファイル

- `lib/types.ts`
- `supabase/schema.sql`

---

## Todo

- [×] `lib/types.ts` を作成
  - [×] `Team` 型を定義（id, name, league, colors）
  - [×] `Player` 型を定義（id, teamId, num, name, furi, pos）
  - [×] `Notice` 型を定義（id, date, text）
  - [×] `league` は `'J1' | 'J2' | 'J3'` のユニオン型
  - [×] `pos` は `'GK' | 'DF' | 'MF' | 'FW'` のユニオン型
  - [×] `colors` は `[string, string, string]` のタプル型
- [×] `supabase/schema.sql` を作成
  - [×] `teams` テーブルを定義（id, name, league check, colors text[]）
  - [×] `players` テーブルを定義（id, team_id FK, num, name, furi, pos check）
  - [×] `notices` テーブルを定義（id, date, text）
  - [×] `players.team_id` に `on delete cascade` を設定
