# 006 - API ルート：チーム

チームの CRUD を行う Route Handler を実装する。

## 対象ファイル

- `app/api/teams/route.ts`

---

## Todo

- [×] `GET` ハンドラを実装
  - [×] `db.getTeams()` で全チームを取得して返す
  - [×] Supabase 未設定時は `defaultData` の `DEFAULT_TEAMS` を返す
  - [×] 認証不要
- [×] `POST` ハンドラを実装
  - [×] 管理者認証チェック（未認証は 401）
  - [×] リクエストボディから name・league・colors を取得
  - [×] `db.createTeam()` でチームを追加して返す
- [×] `PUT` ハンドラを実装
  - [×] 管理者認証チェック（未認証は 401）
  - [×] リクエストボディから id・name・league・colors を取得
  - [×] `db.updateTeam()` でチームを更新して返す
- [×] `DELETE` ハンドラを実装
  - [×] 管理者認証チェック（未認証は 401）
  - [×] `request.nextUrl.searchParams` から `id` を取得
  - [×] `db.deleteTeam()` でチームを削除（所属選手も cascade 削除）
- [×] レスポンスは `Response.json()` を使う（`NextResponse` は使わない）
