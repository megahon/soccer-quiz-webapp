# 013 - 管理者パネル：お知らせ管理

`AdminPanel.tsx` 内のお知らせ管理機能（一覧・追加・削除）を実装する。

## 対象ファイル

- `components/AdminPanel.tsx`（お知らせ管理セクション）

---

## Todo

- [×] お知らせ一覧を表示
  - [×] `GET /api/notices` でお知らせを取得
  - [×] 日付・本文を表示
- [×] お知らせ追加フォームを実装
  - [×] date（日付テキスト or date ピッカー）・text（テキストエリア）
  - [×] `POST /api/notices` に送信
  - [×] 成功後にリストを再取得、Toast で通知
- [×] お知らせ削除を実装
  - [×] 削除ボタンで `DELETE /api/notices?id=X` に送信
  - [×] 成功後にリストを再取得、Toast で通知
- [×] `/admin` ページに管理者ログアウトボタンを設置
  - [×] `POST /api/auth` へ `{ action: 'logout' }` を送信
  - [×] 成功後にページをリロード（AdminLogin 画面に戻る）
