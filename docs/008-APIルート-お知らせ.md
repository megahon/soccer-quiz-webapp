# 008 - API ルート：お知らせ

お知らせの取得・追加・削除を行う Route Handler を実装する。

## 対象ファイル

- `app/api/notices/route.ts`

---

## Todo

- [×] `GET` ハンドラを実装
  - [×] `db.getNotices()` で全お知らせを取得して返す
  - [×] Supabase 未設定時は空配列を返す
  - [×] 認証不要
- [×] `POST` ハンドラを実装
  - [×] 管理者認証チェック（未認証は 401）
  - [×] リクエストボディから date・text を取得
  - [×] `db.createNotice()` でお知らせを追加して返す
- [×] `DELETE` ハンドラを実装
  - [×] 管理者認証チェック（未認証は 401）
  - [×] `request.nextUrl.searchParams` から `id` を取得
  - [×] `db.deleteNotice()` でお知らせを削除
- [×] レスポンスは `Response.json()` を使う（`NextResponse` は使わない）
