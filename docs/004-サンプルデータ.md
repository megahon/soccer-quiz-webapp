# 004 - サンプルデータ

Supabase 未設定時のフォールバックとして使う J1 全 18 チーム・180 名のデフォルトデータを作成する。

## 対象ファイル

- `lib/defaultData.ts`

---

## Todo

- [×] `lib/defaultData.ts` を作成
  - [×] `DEFAULT_TEAMS`: J1 全 18 チームのデータを定義
    - [×] 各チームに id, name, league(`'J1'`), colors（3色）を設定
  - [×] `DEFAULT_PLAYERS`: 各チーム 10 名、計 180 名を定義
    - [×] 内訳：GK×1・DF×3・MF×4・FW×2
    - [×] 各選手に id, teamId, num（背番号）, name, furi（ふりがな）, pos を設定
  - [×] `lib/types.ts` の `Team` / `Player` 型に準拠させる
  - [×] このファイルは読み取り専用データのみ（書き込み処理は含まない）
