# 007 - API ルート：選手

選手の CRUD を行う Route Handler を実装する。

## 対象ファイル

- `app/api/players/route.ts`

---

## Todo

- [×] `GET` ハンドラを実装
  - [×] `db.getPlayers()` で全選手を取得して返す
  - [×] Supabase 未設定時は `defaultData` の `DEFAULT_PLAYERS` を返す
  - [×] 認証不要
- [×] `POST` ハンドラを実装
  - [×] 管理者認証チェック（未認証は 401）
  - [×] リクエストボディから teamId・num・name・furi・pos を取得
  - [×] `db.createPlayer()` で選手を追加して返す
- [×] `PUT` ハンドラを実装
  - [×] 管理者認証チェック（未認証は 401）
  - [×] リクエストボディから id・teamId・num・name・furi・pos を取得
  - [×] `db.updatePlayer()` で選手を更新して返す
- [×] `DELETE` ハンドラを実装
  - [×] 管理者認証チェック（未認証は 401）
  - [×] `request.nextUrl.searchParams` から `id` を取得
  - [×] `db.deletePlayer()` で選手を削除
- [×] レスポンスは `Response.json()` を使う（`NextResponse` は使わない）
